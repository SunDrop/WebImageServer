#include <iostream>

int main() {
    std::cout << "Here will be main code of Web Image Server!" << std::endl;
    /**
     * PseudoCode:
     *
     * wis = new WebImageServer();
     * wis->processQuery(); // inside the main code
     * wis->response->send();
     */
    return 0;
}